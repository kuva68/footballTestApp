import { scaledSize } from '@utils/scaleSize';
import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  mt: { marginTop: scaledSize(12) },
  mt2: { marginTop: scaledSize(24) },
});
